# Version Configuration
# Single source of truth for app versioning

# Marketing version - follows semantic versioning (major.minor.patch)
MARKETING_VERSION=0.1.0

# Build number - MUST always increment, never reuse
# Even for the same MARKETING_VERSION, BUILD_NUMBER must increase
BUILD_NUMBER=1

# Bundle identifier - unique app identifier
BUNDLE_ID=com.gcal-notifier

# Usage in packaging scripts:
#   source version.env
#   /usr/libexec/PlistBuddy -c "Set :CFBundleShortVersionString $MARKETING_VERSION" Info.plist
#   /usr/libexec/PlistBuddy -c "Set :CFBundleVersion $BUILD_NUMBER" Info.plist
#   /usr/libexec/PlistBuddy -c "Set :CFBundleIdentifier $BUNDLE_ID" Info.plist
